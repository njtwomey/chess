node_modules
dist
.DS_Store
*.local
.vite

# Copied out of node_modules by scripts/copy-engine.mjs
public/engine

# Photographs of scoresheets. A scoresheet carries an opponent's full name in
# their own handwriting, and they are not on this team and have not been asked.
# The PGN read off it is committed, into the season's matches.json; the picture
# is not.
games

# Working notes and proposals. Thinking, not decisions: what gets decided ends
# up in the code, the data or CLAUDE.md.
.scratch
